;fun k1
(defun k1 ()
  (f 1))

(defun c () (* 2 2))

(defparameter par-1 1)

(defun b () 
   (r 1)
   (let ((d 9)
     (a 6)
         (b 9))
        (z (f 2))
        (if (* a b)
            (k1)
            (fun2 123))
        (f 3))
  (wrapper1
    (wrapper2
      (when (atom 1)
        (print "Good day!"))))
  (d 2))

  (defun new-func (a) a)